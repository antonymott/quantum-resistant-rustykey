import { describe, test, expect, it } from 'vitest'

// import { loadMlKem1024 } from './index'

describe('#sum', () => {
  // it('returns 0 with no numbers', () => {
  //   expect(add(0, 0)).toBe(0)
  // })
  test('should work', () => {
    expect(true).toBe(true)
  })
})

// it('...', async() => {
//     const recipient = loadMlKem1024()
// })